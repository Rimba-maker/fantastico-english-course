# Fantastico English Course

Multi-page marketing site — Astro 7, Tailwind CSS v4, TypeScript (strict), `motion` for animated React islands.

```sh
npm install
npm run dev
```

Design system: `DESIGN.md`. Full spec: `docs/internal/PRD.md` (not in this repo — gitignored, internal only).
